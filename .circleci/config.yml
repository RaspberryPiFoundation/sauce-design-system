version: 2.1
orbs:
  node: circleci/node@4.5.1
jobs:
  sass-tests:
    docker:
      - image: 'cimg/node:lts'
    steps:
      - checkout
      - node/install-packages
      - run:
          command: npm test
workflows:
  sass-mixin-tests:
    jobs:
      - sass-tests
