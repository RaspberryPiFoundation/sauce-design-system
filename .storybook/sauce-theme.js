import { create } from '@storybook/theming';

export default create({
  base: 'light',
  brandTitle: 'Sauce Design System',
  brandUrl: 'https://sauce.raspberrypi.org',
  fontBase: 'Roboto, sans-serif',
  fontCode: '"Space Mono", monospace',
  textColor: '#222',
  textInverseColor: '#fff'
});
